# EA300P 频率响应算法学习与交付计划

> 制定日期：2026-09-22  
> 最终期限：2026-11-30（不占用 12 月）  
> 目标：在 EA300P 上完成一个可安全运行、可重复验证的自动正弦扫频算法，输出电流环/速度环的幅频、相频和带宽结果。

## 1. 现在所处的位置

已经掌握：

- 电流环、速度环 PI 的基本设计与闭环传递函数推导；
- 二阶系统阻尼比、自然频率的物理含义；
- `Kp`、`Ki` 对系统的作用；
- 正弦扫频测带宽、阶跃测试的基本原理；
- `-3 dB` 带宽的基本概念。

接下来不应继续只做 PI 公式推导。真正需要补齐的是下面这条工程链路：

```text
连续域控制器
  -> 离散实现与延时
  -> 正弦注入和同步采样
  -> 幅值/相位估计
  -> 自动扫频状态机
  -> 带宽提取
  -> 安全保护
  -> 离线、仿真、台架验证
```

建议把时间分配为：学习 30%，编码 40%，实验与复盘 30%。每学完一个知识点，必须在 1～2 天内形成代码、曲线或测试记录，避免“学完再做”。

## 2. 11 月 30 日的交付定义

### 2.1 必须完成（MVP）

- [ ] 可配置测试对象：至少支持速度环；接口预留电流环。
- [ ] 可配置扫频起止频率、每倍频程点数、注入幅值、稳定周期数和测量周期数。
- [ ] 每个频点自动执行：设置频率 -> 等待稳定 -> 采样 -> 计算 -> 保存 -> 下一频点。
- [ ] 用同步检波/单频 DFT 计算输入、输出的复幅值，并得到 `H(jw) = Y/U`。
- [ ] 输出每个频点的频率、增益 dB、相位、有效性标志及异常原因。
- [ ] 自动寻找相对低频增益下降 `3 dB` 的交点，并用相邻点插值得到带宽。
- [ ] 具备过流、过速、饱和、采样异常、停止命令和超时退出保护。
- [ ] 算法不使用动态内存；实时中断内只做固定上限的运算。
- [ ] 有离线测试、仿真测试和实机测试记录，结果可以复现。
- [ ] 有集成说明、参数说明、测试说明和已知限制。

### 2.2 建议完成（时间允许再做）

- [ ] 注入幅值随频率自适应，兼顾信噪比与不饱和。
- [ ] 相位展开、异常频点自动重测。
- [ ] 速度环与电流环共用一套测量内核。
- [ ] 自动导出 CSV，并由脚本生成 Bode 图和测试报告。

### 2.3 暂不纳入本次版本

- 多正弦、chirp、PRBS 等宽带激励；
- 在线自动整定 PI；
- 复杂参数辨识或高阶模型拟合；
- UI 美化和与算法无关的工具链重构。

先把单正弦逐点扫频做准、做稳，再扩展激励方式。

## 3. 需要按顺序补齐的知识

### 3.1 离散控制与数字实现

学习内容：

- 采样频率、控制周期与可测最高频率的关系；
- 零阶保持、计算延时、PWM 更新延时对相位的影响；
- PI 的前向欧拉、后向欧拉、Tustin 离散化差异；
- 限幅、积分饱和与 anti-windup；
- 离散系统极点、稳定性和频率响应。

完成标准：

- 能从当前控制周期推算合理的扫频上限；
- 能解释“理论相位”和“实测相位”之间由采样与延时产生的差异；
- 能用一个简单离散模型复现阶跃曲线和 Bode 曲线。

### 3.2 Bode 图与稳定裕度

学习内容：

- 幅值裕度、相位裕度、增益交越频率；
- 开环、闭环、灵敏度函数之间的区别；
- 共振峰、阻尼比和闭环带宽之间的联系；
- 为什么 `-3 dB` 必须相对低频平台计算，而不是固定相对 0 dB；
- 当响应不是典型低通或有多个交点时，如何定义结果。

完成标准：

- 面对一条实测曲线，能指出低频平台、共振、带宽、噪声区和不可用频点；
- 能明确本算法测的是哪两个信号之间的闭环频响，而不是笼统地说“测带宽”。

### 3.3 单频响应估计

这是本算法的核心，重点掌握：

- 相干采样：测量窗口尽量包含整数个正弦周期；
- 丢弃前若干周期，让瞬态衰减；
- 用同步检波或单频 DFT 提取基波；
- 从输入、输出复幅值计算增益和相位；
- 频谱泄漏、直流偏置、噪声和谐波的影响；
- 相位归一化与相位展开。

推荐的测量核心为：

```text
U = sum(u[n] * exp(-j*theta[n]))
Y = sum(y[n] * exp(-j*theta[n]))
H = Y / U
gain_db = 20 * log10(abs(H))
phase_deg = angle(H) * 180 / pi
```

实现时同时累计输入和输出，最终计算 `Y/U`，不要仅用“设定的注入幅值”代替真实输入，否则限幅、滤波和量化会直接变成测量误差。

完成标准：

- 在离线程序中给定已知增益、相位和噪声，能够恢复原结果；
- 能说明稳定周期数、测量周期数对速度和精度的影响。

### 3.4 嵌入式状态机与数值可靠性

建议状态：

```text
IDLE -> INIT -> SETTLE -> MEASURE -> CALCULATE
     -> NEXT_POINT -> DONE
     -> ABORT / FAULT
```

学习内容：

- 中断层与后台层的职责分离；
- 定点/浮点范围、累计溢出和精度；
- 正弦发生器相位累加器；
- 参数快照、结果缓冲和并发访问；
- 错误码、超时与可恢复停止。

完成标准：

- 每次控制中断的计算量有固定上限；
- 任意状态收到停止或保护信号后，都能在规定时间内撤销注入并退出；
- 重新启动测试不会继承上一次的残留状态。

### 3.5 实验设计与安全

学习内容：

- 注入点和观测点如何决定所测传递函数；
- 空载、带载、不同转速、不同母线电压对结果的影响；
- 如何选择注入幅值，使响应高于噪声又不触发饱和；
- 扫频方向、频率分辨率、驻留时间和总测试时间的权衡；
- 重复性、基准方法和误差预算。

完成标准：

- 台架运行前有书面的参数边界、保护阈值和急停步骤；
- 同一工况连续三次测试的结果满足约定的重复性要求。

## 4. 10 周执行日历

> 默认投入：工作日每天 2 小时，周末半天，约 14～18 小时/周。若是全职投入，可以提前完成，但不要跳过验收门槛。

| 周次 | 日期 | 主题 | 必做任务 | 周交付物与通过标准 |
|---|---|---|---|---|
| W1 | 09-22～09-27 | 冻结需求与接口 | 找到可读的 `SpdResponseTest.c/.h`；画出现有速度环、电流环信号链；确认注入点、输入量、输出量、控制周期、数据通道、保护阈值；记录当前手工扫频流程 | `01_REQUIREMENTS.md`、信号链图、接口表；任何人都能据此回答“测什么、在哪里注入、在哪里采样” |
| W2 | 09-28～10-04 | 离散模型与频率边界 | 补齐离散 PI、ZOH 和一拍延时；建立简化电流环/速度环模型；生成理论 Bode 与阶跃结果；制定频率表 | `02_MODEL_AND_RANGE.md`、理论曲线；明确起止频率、采样率约束和预期带宽范围 |
| W3 | 10-05～10-11 | 离线估计器原型 | 生成带偏置、噪声、谐波的正弦数据；实现同步检波；计算 `Y/U`、dB 和相位；研究非整数周期误差 | 可自动运行的离线测试；中频段建议目标：增益误差不超过 0.2 dB、相位误差不超过 2° |
| W4 | 10-12～10-18 | 嵌入式骨架 | 定义配置、结果、状态、错误码和 API；实现频率表、相位累加器、状态机；划分中断与后台计算 | 算法骨架可编译；用模拟信号可完整跑完 `INIT` 到 `DONE`；可随时 `ABORT` |
| W5 | 10-19～10-25 | 单频闭环打通 | 接入一个安全注入点和输入/输出采样点；实现稳定周期与测量周期；检查实际采样时间戳和符号方向 | 在一个低风险频点得到稳定的幅值和相位；连续 10 次启动/停止无残留注入 |
| W6 | 10-26～11-01 | 单频准确性与异常判定 | 与示波器/现有手工方法对比；加入输入过小、输出饱和、超时、保护触发、数值异常判定 | 至少 5 个频点对比记录；每种异常都有明确状态码，且能安全退出 |
| W7 | 11-02～11-08 | 自动扫频与数据输出 | 实现对数频率表、逐点运行、结果缓存/导出；输出 CSV；绘制 Bode 图 | 一键跑完整段扫频；结果包含频率、dB、相位、有效标志、异常原因和工况元数据 |
| W8 | 11-09～11-15 | 带宽与鲁棒性 | 实现低频平台估计、`-3 dB` 交点插值、相位展开；加入重测策略或坏点标记；评估总耗时 | 对已知模型的带宽误差建议不超过 5%；多交点或无交点时不输出误导性数字 |
| W9 | 11-16～11-22 | 台架验证 | 按测试矩阵跑空载/带载、多个转速、多个注入幅值；每种工况重复 3 次；比较阶跃结果与扫频结果 | 测试报告；建议重复性目标：增益不超过 0.5 dB、相位不超过 3°，保护零漏触发 |
| W10 | 11-23～11-29 | 收口与发布 | 代码审查、边界检查、CPU/内存评估；完善集成、参数、测试和回退说明；冻结版本 | 发布候选版本、完整文档、可复现实验数据；从干净环境按文档可重新得到结果 |
| 缓冲 | 11-30 | 最终验收 | 只处理阻塞发布的问题，不增加功能 | 签署验收清单、打版本标签；未完成项进入下一版本列表 |

## 5. 每周固定工作节奏

每周使用同一种节奏，减少“看了很多但没有产出”的情况：

### 周一：定义本周唯一目标

- 写下本周唯一可演示结果，例如“在已知相位为 -45° 的信号上测得 -45° ± 2°”；
- 把任务拆成最多 5 项；
- 明确周五如何验收。

### 周二、周三：学习后立即编码

- 每次只学能解决当前任务的内容，单次控制在 60～90 分钟；
- 随后立即写最小实验或单元测试；
- 保存输入参数、结果曲线和结论。

### 周四：集成与异常测试

- 接入真实模块或更接近真实的数据；
- 主动测试零输入、噪声、限幅、中途停止、超时、错误参数等异常路径。

### 周五：演示与复盘

- 从头运行一次本周演示；
- 只记录事实：完成了什么、误差是多少、还差什么；
- 未通过验收的任务优先进入下周，不用“完成百分比”掩盖阻塞。

### 周末：整理文档与下周准备

- 把临时笔记整理成仓库文档；
- 保存一组可重复使用的测试数据；
- 预先准备下周的输入、仪器和台架时间。

## 6. 算法设计草案

### 6.1 建议接口

接口名可按现有工程规范调整，但职责应保持清晰：

```c
FreqResp_Init();                 // 初始化模块
FreqResp_Start(&config);         // 校验参数并启动
FreqResp_RealtimeStep(in, out);  // 每个控制周期调用
FreqResp_BackgroundTask();       // 非实时计算、换点和输出
FreqResp_Abort(reason);          // 撤销注入并安全退出
FreqResp_GetStatus();            // 查询状态和进度
FreqResp_GetResult(index);       // 读取频点结果
```

配置至少包括：

- 测试对象和注入点；
- 采样周期；
- 起止频率或频率表；
- 注入幅值；
- 稳定周期数和测量周期数；
- 电流、速度、指令幅值和超时上限；
- 结果容量和数据输出方式。

每个频点结果至少包括：

- 实际频率；
- 输入基波幅值与相位；
- 输出基波幅值与相位；
- 增益 dB 与相位差；
- 样本数、饱和计数、保护状态；
- 有效标志与错误码。

### 6.2 实时层原则

- 用相位累加器生成正弦参考，避免在中断中反复做昂贵初始化；
- 每周期只更新相位、生成注入、累计 `sin/cos` 投影和安全计数；
- `atan2`、`sqrt`、`log10`、插值、相位展开等放到后台任务；
- 对累计器做范围分析，避免长窗口溢出；
- 退出时先清零注入，再切换状态。

### 6.3 每个频点的测量流程

1. 检查频率、幅值和工况是否允许测试。
2. 清空累计器，注入相位从确定值开始。
3. 运行 `N_settle` 个周期，但不参与统计。
4. 运行 `N_measure` 个完整周期并累计输入、输出的正交分量。
5. 检查饱和、输入幅值、样本数和保护状态。
6. 计算 `H = Y/U`，得到 dB 与相位。
7. 保存原始统计量和最终结果，再进入下一频点。
8. 任意阶段出现保护或停止命令，立刻撤销注入并进入 `ABORT/FAULT`。

### 6.4 频率表建议

- 使用对数间隔，初版每倍频程 4～8 点；
- 起始频率应明显低于预期带宽，以取得低频平台；
- 结束频率需覆盖预期带宽之后一段，但必须低于采样、控制和机械安全限制；
- 带宽附近可以二次加密频点，不必一开始全频段高密度扫描；
- 每个频点尽量测整数周期；低频点总耗时过长时，单独设最大驻留时间并记录实际周期数。

### 6.5 `-3 dB` 带宽提取

1. 从最低的若干个有效频点估计低频平台 `G0`，优先用中位数抑制异常点。
2. 目标电平为 `G0 - 3.0103 dB`。
3. 找到第一次从目标电平上方向下穿越的两个有效频点。
4. 在对数频率轴上插值得到带宽。
5. 如果没有交点、交点前有明显异常点或曲线不是预期低通形态，则返回“无有效带宽”，同时保留曲线供人工判断。

## 7. 验证路线

### 7.1 第一级：纯数学测试

输入人工构造的正弦信号，覆盖：

- 不同幅值和相位；
- 直流偏置；
- 白噪声；
- 二次、三次谐波；
- 非整数周期窗口；
- 极小输入、零输入和浮点异常。

目的：证明估计器本身正确。

### 7.2 第二级：已知离散系统

将激励送入一阶、二阶和含纯延时的离散模型，将实测结果与理论 `H(e^jwT)` 比较。

建议验收门槛：

- 通带和带宽附近增益误差不超过 0.5 dB；
- 相位误差不超过 3°；
- 带宽误差不超过 5%；
- 对无 `-3 dB` 交点的数据正确报告“无有效带宽”。

目的：证明完整扫频流程正确。

### 7.3 第三级：控制器仿真或 HIL

- 使用与 EA300P 相同的控制周期、限幅和 PI 参数；
- 加入一拍延时、量化、噪声和饱和；
- 测试中途停止、保护触发和通信丢失。

目的：在不上真实电机前发现集成和安全问题。

### 7.4 第四级：台架测试

首次上台架必须从低注入、低风险频点开始，先单频再扫频。

建议最小测试矩阵：

| 维度 | 最小取值 |
|---|---|
| 工况 | 空载、典型负载 |
| 转速 | 低速、中速、额定工作点附近 |
| 注入幅值 | 小、中两档 |
| 方法 | 自动扫频、现有手工扫频/仪器基准 |
| 重复次数 | 每个关键工况 3 次 |

台架验收不仅看“曲线像不像”，还要检查：

- 重复性；
- 是否出现电流/速度饱和；
- 实际频率是否正确；
- CPU 占用和控制周期是否超时；
- 异常退出是否清除注入；
- 阶跃测试估计出的动态快慢是否与扫频带宽趋势一致。

## 8. 风险清单与预案

| 风险 | 最早信号 | 预案 |
|---|---|---|
| 基线源码不可读或接口不清楚 | W1 仍说不清注入点和采样点 | W1 立即协调可读代码、接口文档或原作者；未解决前不盲写集成代码 |
| 低频扫描耗时过长 | 单次全扫超过可接受台架时间 | 降低低频点密度；减少稳定/测量周期但保留最小统计要求；先粗扫再局部细扫 |
| 高频信噪比差 | 幅相抖动大、重复性差 | 提高安全范围内的注入幅值、增加测量周期、标记无效点，不用平滑掩盖坏数据 |
| 注入导致饱和或保护 | 输入/输出削顶、饱和计数增加 | 立即撤销注入；降低幅值；必要时按频率自适应幅值 |
| 相位与理论差异大 | 相位误差随频率近似线性增加 | 检查采样时间对齐、计算/PWM 延时、输入输出通道延时和符号定义 |
| 带宽结果漂移 | 三次扫频的交点变化明显 | 增加带宽附近频点、提高测量周期、检查工况漂移和温升 |
| 到 11 月还在加功能 | MVP 尚未闭环却讨论新激励方法 | 严格冻结范围，先交付单正弦逐点扫频 |

## 9. 决策日志：开始编码前必须写清楚

在 `01_REQUIREMENTS.md` 中逐项填写，不允许留成口头约定：

- [ ] 测量对象：速度闭环、电流闭环，还是其他传递函数？
- [ ] 注入点的变量名、单位、量纲和限幅位置是什么？
- [ ] 输入测量值用“注入命令”还是限幅/滤波后的实际输入？
- [ ] 输出点的变量名、单位和采样时刻是什么？
- [ ] 控制周期、PWM 周期、速度环周期分别是多少？
- [ ] 算法运行在哪个任务/中断？计算预算是多少？
- [ ] 频率范围和预期带宽是多少？
- [ ] 允许的最大电流、速度波动、转矩波动是多少？
- [ ] 数据怎样导出？单次最多保存多少频点？
- [ ] 与哪一种现有方法或仪器结果对比？
- [ ] 谁确认台架可以运行，谁确认结果可以发布？

如果这些问题没有答案，优先找答案，不要用代码猜接口。

## 10. 前 3 天立即执行

### Day 1：09-22

- [ ] 获取可读的 `SpdResponseTest.c` 和 `SpdResponseTest.h`，确认其版本与目标固件一致；
- [ ] 找出速度给定、速度反馈、`Iq` 给定/反馈以及 PI 输出的变量；
- [ ] 记录电流环、速度环、PWM 的实际调用周期；
- [ ] 画一张信号链草图，标记候选注入点和采样点。

### Day 2：09-23

- [ ] 与算法负责人确认要测量的传递函数定义；
- [ ] 确认扫频范围、预期带宽、台架保护值和数据输出方式；
- [ ] 建立 `01_REQUIREMENTS.md`，把第 9 节的问题全部写入；
- [ ] 选定第一版只支持的一个环路，建议优先打通速度环。

### Day 3：09-24

- [ ] 建立最简单的离散闭环模型；
- [ ] 生成理论阶跃和 Bode 数据；
- [ ] 制作第一版对数频率表；
- [ ] 把理论带宽、扫频上限、每点周期数和预计总耗时写入模型说明。

Day 3 结束时必须能够用一句无歧义的话描述任务，例如：

> 在速度给定端叠加正弦，采集限幅后的实际速度指令与速度反馈，计算闭环 `SpeedFeedback/SpeedCommand` 的幅相响应，并在指定工况下估计相对低频平台的 `-3 dB` 带宽。

## 11. 最终验收清单

### 功能

- [ ] 配置合法时可完整扫频并正常结束；
- [ ] 中途停止、保护和超时能安全撤销注入；
- [ ] 可输出每个频点的完整结果和状态；
- [ ] 可计算有效的 `-3 dB` 带宽，无法计算时明确报错。

### 准确性

- [ ] 已知正弦测试通过；
- [ ] 已知离散系统测试通过；
- [ ] 与现有方法/仪器的关键频点对比通过；
- [ ] 台架关键工况三次重复性通过。

### 实时性与安全

- [ ] 无动态内存；
- [ ] 无累计溢出和除零路径；
- [ ] 实时任务最坏执行时间满足预算；
- [ ] 所有退出路径都会清除注入；
- [ ] 参数越界不会启动测试；
- [ ] 原有控制和保护功能未被破坏。

### 可维护性

- [ ] 配置、状态、结果和错误码有明确注释；
- [ ] 核心计算与硬件接口解耦；
- [ ] 离线测试可一键重复运行；
- [ ] 文档包含集成方法、参数说明、测试步骤、已知限制和回退方案；
- [ ] 发布提交和版本标签可追溯。

## 12. 每周复盘模板

每周五复制下面内容填写，控制在一页内：

```text
本周唯一目标：
验收方法：
验收结果（通过/未通过）：

实际完成：
1.
2.
3.

关键数据：
- 增益误差：
- 相位误差：
- 带宽误差：
- 重复性：
- 最长执行时间：

发现的问题及证据：
1.
2.

下周唯一目标：
阻塞项与负责人：
```

## 13. 执行原则

1. 先定义传递函数，再写扫频代码。
2. 先验证一个频点，再扩展到全频段。
3. 先在已知模型上证明算法正确，再上台架。
4. 保存原始统计量和工况信息，不只保存最终带宽数字。
5. 无效数据要明确标记，不能靠平滑把问题隐藏起来。
6. 每周必须有可运行、可测量、可复现的交付物。
7. 11 月 15 日之后原则上不增加功能，只修正确性、安全性和文档问题。

